#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(int argc, char *argv[]) {

    int x;

    x=10;
    while(x > 0) {
        printf("Number: %d\n", x);
        x--;
    }

    return (0);
}